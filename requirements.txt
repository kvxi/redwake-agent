# Runtime dependencies for the Redwake coding agent.
# Standard-library imports (json, os, re, subprocess, pathlib, typing,
# datetime, urllib.parse, unittest) require no entries here.

# source/loop.py - Anthropic API client and Message types
anthropic>=1.0.0

# source/loop.py - load_dotenv() for reading .env
python-dotenv>=1.2.3

# tools/tool_set.py - search() and fetch() HTTP calls
requests>=2.34.2

# tools/tool_set.py - HTML parsing in fetch()
beautifulsoup4>=4.15.0

# tools/tool_set.py - HTML to Markdown conversion in fetch()
markdownify>=1.2.3
